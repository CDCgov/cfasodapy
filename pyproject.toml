[project]
name = "cfasodapy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Scott Olesen", email = "ulp7@cdc.gov" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
cfasodapy = "cfasodapy:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
